<UserControl x:Class="ValheimServerManager.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:ValheimServerManager.Views"
             xmlns:vm="clr-namespace:ValheimServerManager.ViewModels"
             Background="{StaticResource WindowBackgroundBrush}">
    <UserControl.DataContext>
        <vm:DashboardViewModel />
    </UserControl.DataContext>

    <Grid Margin="24,18,24,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="10" Height="10" Fill="{StaticResource SuccessBrush}" />
                <TextBlock Text=" Server is Running" Margin="10,0,0,0" FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" />
                <TextBlock Text="• Uptime: 2h 14m" Margin="16,0,0,0" Foreground="{StaticResource SecondaryTextBrush}" FontSize="13" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Start" Style="{StaticResource PrimaryActionButtonStyle}" />
                <Button Content="Stop" Style="{StaticResource SecondaryActionButtonStyle}" />
                <Button Content="Restart" Style="{StaticResource SecondaryActionButtonStyle}" />
                <Button Content="Update" Style="{StaticResource AccentActionButtonStyle}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Players: 3 / 10" Foreground="{StaticResource SecondaryTextBrush}" FontSize="13" />
                    <TextBlock Text="14 online" Foreground="{StaticResource PrimaryTextBrush}" FontSize="18" FontWeight="SemiBold" Margin="0,8,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="World" Foreground="{StaticResource SecondaryTextBrush}" FontSize="13" />
                    <TextBlock Text="MyWorld" Foreground="{StaticResource PrimaryTextBrush}" FontSize="18" FontWeight="SemiBold" Margin="0,8,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Mode" Foreground="{StaticResource SecondaryTextBrush}" FontSize="13" />
                    <TextBlock Text="Survival" Foreground="{StaticResource PrimaryTextBrush}" FontSize="18" FontWeight="SemiBold" Margin="0,8,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="3">
                    <TextBlock Text="Version" Foreground="{StaticResource SecondaryTextBrush}" FontSize="13" />
                    <TextBlock Text="1.0.14" Foreground="{StaticResource PrimaryTextBrush}" FontSize="18" FontWeight="SemiBold" Margin="0,8,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*" />
                <ColumnDefinition Width="1.05*" />
                <ColumnDefinition Width="1.15*" />
            </Grid.ColumnDefinitions>

            <views:ServerInformationPanel Grid.Column="0" Margin="0,0,12,0" />
            <views:WorldModifiersPanel Grid.Column="1" Margin="0,0,12,0" />
            <views:ServerStatusPanel Grid.Column="2" />
        </Grid>

        <Grid Grid.Row="3" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*" />
                <ColumnDefinition Width="1.0*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,12,0">
                <views:ModsPanel />
            </StackPanel>

            <StackPanel Grid.Column="1">
                <views:PlayerListPanel />
                <views:ConsolePanel Margin="0,14,0,0" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
