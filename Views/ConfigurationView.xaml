<UserControl x:Class="ValheimServerManager.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ValheimServerManager.ViewModels"
             Background="{DynamicResource WindowBackgroundBrush}">
    <UserControl.DataContext><vm:ConfigurationViewModel /></UserControl.DataContext>
    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="{DynamicResource WindowBackgroundBrush}">
        <Grid Margin="24,18,24,28" Background="{DynamicResource WindowBackgroundBrush}">
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0,0,0,22">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="⚙  Configuration" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" />
                    <TextBlock Text="Application, server and advanced settings" Margin="0,5,0,0" Foreground="{DynamicResource SecondaryTextBrush}" />
                </StackPanel>
                <Button Grid.Column="1" Content="💾  Save Configuration" Command="{Binding SaveConfigurationCommand}" Style="{StaticResource SecondaryActionButtonStyle}" />
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource ConfigCardStyle}" Margin="0,0,12,12"><StackPanel>
                    <TextBlock Text="▣  Server Paths" Style="{StaticResource ConfigCardTitleStyle}" /><TextBlock Text="Server Directory" Style="{StaticResource FieldLabelStyle}" />
                    <Grid Margin="0,0,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="34" /></Grid.ColumnDefinitions><TextBox Text="{Binding ServerDirectory, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ConfigTextBoxStyle}" /><Button Grid.Column="1" Content="📂" Command="{Binding BrowseServerDirectoryCommand}" Style="{StaticResource SecondaryActionButtonStyle}" Width="30" MinWidth="0" Padding="0" Margin="4,0,0,0" /></Grid>
                    <TextBlock Text="Server Executable" Style="{StaticResource FieldLabelStyle}" /><Grid Margin="0,0,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="34" /></Grid.ColumnDefinitions><TextBox Text="{Binding ServerExecutable, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ConfigTextBoxStyle}" /><Button Grid.Column="1" Content="📂" Command="{Binding BrowseExecutableCommand}" Style="{StaticResource SecondaryActionButtonStyle}" Width="30" MinWidth="0" Padding="0" Margin="4,0,0,0" /></Grid>
                    <TextBlock Text="World" Style="{StaticResource FieldLabelStyle}" /><ComboBox ItemsSource="{Binding WorldOptions}" SelectedItem="{Binding SelectedWorld, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ConfigComboBoxStyle}" /><TextBlock Text="New world name" Style="{StaticResource FieldLabelStyle}" /><TextBox Text="{Binding NewWorldName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ConfigTextBoxStyle}" /><Button Content="↻  Refresh worlds" Command="{Binding RefreshWorldsCommand}" Style="{StaticResource SecondaryActionButtonStyle}" HorizontalAlignment="Left" Margin="0,8,0,0" />
                    <TextBlock Text="Worlds directory (local saves)" Style="{StaticResource FieldLabelStyle}" /><TextBox Text="{Binding WorldsDirectory}" IsReadOnly="True" Style="{StaticResource ConfigTextBoxStyle}" /><TextBlock Text="Backups Directory" Style="{StaticResource FieldLabelStyle}" /><TextBox Text="{Binding BackupsDirectory}" Style="{StaticResource ConfigTextBoxStyle}" />
                </StackPanel></Border>
                <Border Grid.Column="1" Style="{StaticResource ConfigCardStyle}" Margin="0,0,12,12"><StackPanel><TextBlock Text="▶  Server Startup" Style="{StaticResource ConfigCardTitleStyle}" /><TextBlock Text="Startup Arguments" Style="{StaticResource FieldLabelStyle}" /><TextBox Text="{Binding StartupArguments, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ConfigTextBoxStyle}" /><CheckBox Content="Auto start server on application launch" IsChecked="{Binding AutoStart}" Style="{StaticResource ConfigCheckBoxStyle}" /><CheckBox Content="Minimize to tray when server is running" IsChecked="{Binding MinimizeToTray}" Style="{StaticResource ConfigCheckBoxStyle}" /><CheckBox Content="Close to tray instead of exit" IsChecked="{Binding CloseToTray}" Style="{StaticResource ConfigCheckBoxStyle}" /><CheckBox Content="Check for server updates on startup" IsChecked="{Binding CheckForUpdates}" Style="{StaticResource ConfigCheckBoxStyle}" /></StackPanel></Border>
                <Border Grid.Column="2" Style="{StaticResource ConfigCardStyle}" Margin="0,0,0,12"><StackPanel><TextBlock Text="▣  Application" Style="{StaticResource ConfigCardTitleStyle}" /><TextBlock Text="Accent Color" Style="{StaticResource FieldLabelStyle}" /><ComboBox ItemsSource="{Binding AccentColors}" SelectedItem="{Binding SelectedAccentColor}" Style="{StaticResource ConfigComboBoxStyle}" /><TextBlock Text="Language" Style="{StaticResource FieldLabelStyle}" /><ComboBox ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage}" Style="{StaticResource ConfigComboBoxStyle}" /><CheckBox Content="Check for updates automatically" IsChecked="{Binding CheckForUpdates}" Style="{StaticResource ConfigCheckBoxStyle}" /></StackPanel></Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
