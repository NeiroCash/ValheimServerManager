<UserControl x:Class="ValheimServerManager.Views.NavigationSidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource SidebarBackgroundBrush}">
    <Border Background="{DynamicResource SidebarBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="0,0,1,0">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
            <StackPanel Margin="18,22,12,0">
                <Button Content="Dashboard" Command="{Binding ShowDashboardCommand}" Style="{StaticResource SidebarNavButtonStyle}" />
                <Button Content="World Settings" Style="{StaticResource SidebarNavButtonStyle}" IsEnabled="False" />
                <Button Content="Mods" Style="{StaticResource SidebarNavButtonStyle}" IsEnabled="False" />
                <Button Content="Console / Logs" Style="{StaticResource SidebarNavButtonStyle}" IsEnabled="False" />
                <Button Content="Players" Style="{StaticResource SidebarNavButtonStyle}" IsEnabled="False" />
                <Button Content="Backups" Style="{StaticResource SidebarNavButtonStyle}" IsEnabled="False" />
                <Button Content="Schedules" Style="{StaticResource SidebarNavButtonStyle}" IsEnabled="False" />
                <Button Content="Configuration" Command="{Binding ShowConfigurationCommand}" Style="{StaticResource SidebarNavButtonStyle}" />
            </StackPanel>
            <Border Grid.Row="1" Padding="12,12,12,16" Margin="12,0,12,16"
                    Background="{DynamicResource CardBackgroundBrush}" CornerRadius="10"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Server connected" FontSize="11" Foreground="{DynamicResource MutedTextBrush}" />
                    <TextBlock Text="D:/Games/ValheimServer/" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,6,0,0" TextTrimming="CharacterEllipsis" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
